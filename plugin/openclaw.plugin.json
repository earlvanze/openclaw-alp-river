{
  "id": "openclaw-alp-river",
  "name": "OpenClaw Alp River",
  "description": "Advisory S/M/L/XL workflow tier guardrails for OpenClaw agents.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": { "type": "boolean", "default": true },
      "minimumRiverTier": { "type": "string", "enum": ["S", "M", "L", "XL"], "default": "M" },
      "injectPromptContext": { "type": "boolean", "default": true },
      "debug": { "type": "boolean", "default": false }
    }
  },
  "uiHints": {
    "enabled": { "label": "Enable Alp River workflow guidance" },
    "minimumRiverTier": { "label": "Minimum tier for River workflow", "help": "Tasks at this tier or higher get specialist fan-out / review-gate guidance." },
    "injectPromptContext": { "label": "Inject prompt context", "help": "Adds a short workflow advisory block during prompt build." },
    "debug": { "label": "Debug logging", "advanced": true }
  }
}
