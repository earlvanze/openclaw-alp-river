# OpenClaw Alp River Plugin Prototype

Prototype plugin artifacts for Alp River-style workflow guidance.

Status: branch-only prototype. Do not treat this as a separate product repo unless promoted later.

The plugin is advisory only:
- classifies prompts into S/M/L/XL workflow tiers
- injects compact `before_prompt_build` guidance
- does not spawn subagents directly
- keeps OpenClaw as orchestrator
